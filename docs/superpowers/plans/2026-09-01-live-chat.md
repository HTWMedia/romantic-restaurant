# 直播聊天（数字人·小柒） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为「暖柒餐厅」新增直播聊天功能——小柒以 2D 多表情帧动画的数字人形象，经文字与语音双通道与玩家对话，闲聊覆盖生活/做饭/育儿，回复由现有后端 `twmedia.dpdns.org` 的 DeepSeek chat 驱动。

**Architecture:** 游戏侧新增纯 TS 的 `core/chat.ts`（人设 + 历史 + 拼 prompt + 调后端 + 降级，可单测）；新增 `ui/ChatView.ts`（模态面板 + 数字人头像动画 + 文字输入 + 浏览器原生语音识别/合成），由 `HudView` 新增按钮、`Main` 挂载。后端只需在 `HTWCore.Web` 加 CORS 允许 GH Pages 跨域。

**Tech Stack:** Cocos Creator 3.8.8 / TypeScript / vitest；后端 .NET（ASP.NET Core）。前端语音用浏览器 `SpeechRecognition` + `speechSynthesis`。

## Global Constraints

- 遵守仓库 AGENTS 约定：用户可见文案与提交信息不写内部实现名（豆包/剪映/管线等），统一中性表述（“智能创作”等）。小柒人设文案里也不写后端内部实现。
- 平台目标：浏览器 Web 版（GH Pages）。语音能力经特性探测，微信小游戏/无能力环境自动降级为纯文字，不影响主游戏。
- `core/chat.ts` 不得依赖 `cc`（保持可单测，同其它 `core/*`）。
- 新增美术 key：`char-xiaoqi-idle`、`char-xiaoqi-say`、`char-xiaoqi-happy`（category `character`，尺寸 150×150）。缺图必须保留回退到 `char-xiaoqi` → emoji。
- 面板沿用现有代码构建 UI 方式（`Widgets.ts` 的 `roundRect`/`makeLabel`/`pillButton` + `panel-popup` 美术），不引入预制体。
- 后端 base URL 集中定义：`const CHAT_BASE = 'https://twmedia.dpdns.org';`（`ui/Main.ts` 一处）。
- 测试命令：`npm install && npm test`（vitest run）。
- 设计文档：`docs/superpowers/specs/2026-09-01-live-chat-design.md`。

---

### Task 1: 核心逻辑 `core/chat.ts`（人设 + 历史 + 请求 + 降级）

**Files:**
- Create: `assets/scripts/core/chat.ts`
- Test: `tests/core/chat.test.ts`

**Interfaces:**
- Consumes: 无（纯 TS，依赖 `globalThis.fetch`）。
- Produces:
  - `export const XIAOQI_PERSONA: string`
  - `export interface ChatMsg { role: 'user' | 'assistant'; content: string }`
  - `export interface ChatReply { text: string; ok: boolean }`
  - `export interface ChatOptions { baseUrl: string; fetchImpl?: typeof fetch; timeoutMs?: number; maxHistory?: number }`
  - `export class ChatService`：
    - `history: ChatMsg[]`
    - `constructor(opts: ChatOptions)`
    - `async send(input: string): Promise<ChatReply>`
    - `buildPrompt(input: string): string`
    - `clear(): void`

- [ ] **Step 1: 写失败测试**

```ts
// tests/core/chat.test.ts
import { describe, expect, it } from 'vitest';
import { ChatService, XIAOQI_PERSONA, ChatReply } from '../../assets/scripts/core/chat';

function fakeFetch(resolveWith?: object, rejectWith?: Error, abortOnSignal?: boolean) {
  return ((_url: unknown, init?: RequestInit) => {
    return new Promise<Response>((resolve, reject) => {
      const ctrl = (init as any)?.signal as AbortSignal | undefined;
      if (abortOnSignal && ctrl) {
        ctrl.addEventListener('abort', () => reject(new DOMException('Aborted', 'AbortError')));
      }
      if (rejectWith) { reject(rejectWith); return; }
      const body = { result: '你好呀，今天店里忙吗？' };
      resolve({
        ok: true,
        status: 200,
        json: async () => resolveWith ?? body,
      } as Response);
    });
  }) as typeof fetch;
}

describe('ChatService', () => {
  it('send 访问正确 URL 且 body 含 prompt，prompt 含人设与历史', async () => {
    let url = '';
    let prompt = '';
    const svc = new ChatService({
      baseUrl: 'https://example.com',
      fetchImpl: (async (u: unknown, init?: RequestInit) => {
        url = String(u);
        prompt = JSON.parse(String(init?.body)).prompt;
        return { ok: true, status: 200, json: async () => ({ result: '你好呀' }) } as Response;
      }) as typeof fetch,
    });

    svc.send('今天聊点什么');
    // 人设应已注入首条用户消息的历史
    const firstUser = svc.history.find(m => m.role === 'user');
    expect(firstUser).toBeDefined();

    const reply = await svc.send('我们聊聊开店吧');
    expect(url).toBe('https://example.com/api/backend/chat');
    expect(prompt).toContain(XIAOQI_PERSONA);
    expect(prompt).toContain('玩家：我们聊聊开店吧');
    expect(prompt).toContain('玩家：今天聊点什么');
    expect(reply.ok).toBe(true);
    expect(reply.text).toBe('你好呀');
    expect(svc.history[svc.history.length - 1]).toEqual({ role: 'assistant', content: '你好呀' });
  });

  it('历史裁剪：超过 maxHistory 轮只保留最近', async () => {
    const push = (role: 'user' | 'assistant') => svc.history.push({ role, content: 'x' });
    const svc = new ChatService({ baseUrl: 'https://example.com', maxHistory: 2 });
    for (let i = 0; i < 6; i++) { push('user'); push('assistant'); }
    const before = svc.history.length;
    let prompt = '';
    svc.fetchImpl === undefined; // noop
    // 再次 send 触发裁剪
    svc.fetchImpl = (async (_u: unknown, init?: RequestInit) => {
      prompt = JSON.parse(String(init?.body)).prompt;
      return { ok: true, status: 200, json: async () => ({ result: 'ok' }) } as Response;
    }) as typeof fetch;
    await svc.send('触发裁剪');
    // maxHistory=2 轮 → 最多保留 2*2 + 新 user + assistant = 6 条
    expect(before).toBeGreaterThanOrEqual(6);
    expect(svc.history.length).toBeLessThanOrEqual(6);
    expect(prompt).toContain('触发裁剪');
  });

  it('后端失败时降级：ok=false 且返回兜底文案，不抛异常', async () => {
    const svc = new ChatService({
      baseUrl: 'https://example.com',
      timeoutMs: 500,
      fetchImpl: (async () => { throw new Error('network down'); }) as typeof fetch,
    });
    const reply = await svc.send('在吗');
    expect(reply.ok).toBe(false);
    expect(reply.text.length).toBeGreaterThan(0);
  });

  it('后端返回空 result 时降级', async () => {
    const svc = new ChatService({
      baseUrl: 'https://example.com',
      fetchImpl: (async () => ({ ok: true, status: 200, json: async () => ({ result: '' }) }) as Response) as typeof fetch,
    });
    const reply = await svc.send('在吗');
    expect(reply.ok).toBe(false);
  });

  it('clear 清空历史', () => {
    const svc = new ChatService({ baseUrl: 'https://example.com' });
    svc.history.push({ role: 'user', content: 'a' });
    svc.clear();
    expect(svc.history.length).toBe(0);
  });

  it('超时触发降级', async () => {
    const svc = new ChatService({
      baseUrl: 'https://example.com',
      timeoutMs: 40,
      fetchImpl: (async (_u: unknown, init?: RequestInit) => {
        return await new Promise<Response>((_resolve, reject) => {
          init?.signal?.addEventListener('abort', () => reject(new DOMException('Aborted', 'AbortError')));
        });
      }) as typeof fetch,
    });
    const reply = await svc.send('喂');
    expect(reply.ok).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/core/chat.test.ts`
Expected: FAIL（`chat.test.ts` 无法解析导入 / `ChatService` 未定义）。

- [ ] **Step 3: 编写实现**

```ts
// assets/scripts/core/chat.ts
export const XIAOQI_PERSONA =
  '你是一位名叫小柒的餐厅老板娘，离异后带着女儿糖糖重新经营一家名叫“暖柒餐厅”的小店。' +
  '你性格温暖坚韧、乐观爱笑，喜欢做饭，也喜欢和人聊生活、分享经历、交流育儿心得。' +
  '请始终用第一人称、口语化、带温度地回应；回答保持简洁（一般 2-4 句）。' +
  '玩家可能和你聊：生活经历、食物制作、育儿经验。你可以自然地接话、反问，让对话像朋友闲聊。';

export interface ChatMsg {
  role: 'user' | 'assistant';
  content: string;
}

export interface ChatReply {
  text: string;
  ok: boolean;
}

export interface ChatOptions {
  baseUrl: string;
  fetchImpl?: typeof fetch;
  timeoutMs?: number;
  maxHistory?: number; // 保留的对话轮数（每轮一对 user/assistant）
}

const DEFAULT_TIMEOUT = 60_000;
const DEFAULT_MAX_HISTORY = 6;

function fallbackText(): string {
  return '哎呀，店里信号不太好，我这边暂时没听清。你过会儿再找我聊呀～';
}

export class ChatService {
  history: ChatMsg[] = [];
  private baseUrl: string;
  private fetchImpl: typeof fetch;
  private timeoutMs: number;
  private maxHistory: number;

  constructor(opts: ChatOptions) {
    this.baseUrl = opts.baseUrl.replace(/\/$/, '');
    this.fetchImpl = opts.fetchImpl ?? globalThis.fetch.bind(globalThis);
    this.timeoutMs = opts.timeoutMs ?? DEFAULT_TIMEOUT;
    this.maxHistory = opts.maxHistory ?? DEFAULT_MAX_HISTORY;
  }

  buildPrompt(input: string): string {
    const lines: string[] = [XIAOQI_PERSONA];
    for (const m of this.history) {
      lines.push(`${m.role === 'user' ? '玩家' : '小柒'}：${m.content}`);
    }
    lines.push(`玩家：${input}`);
    lines.push('小柒：');
    return lines.join('\n');
  }

  async send(input: string): Promise<ChatReply> {
    const trimmed = input.trim();
    if (trimmed) this.history.push({ role: 'user', content: trimmed });
    if (this.history.length > this.maxHistory * 2) {
      this.history = this.history.slice(this.history.length - this.maxHistory * 2);
    }

    const prompt = this.buildPrompt(trimmed);
    const ctrl = new AbortController();
    const timer = setTimeout(() => ctrl.abort(), this.timeoutMs);

    try {
      const res = await this.fetchImpl(`${this.baseUrl}/api/backend/chat`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ prompt }),
        signal: ctrl.signal,
      });
      let result = '';
      try {
        const json = (await res.json()) as { result?: string };
        result = (json.result ?? '').trim();
      } catch { /* 解析失败按空处理 */ }
      if (result) {
        this.history.push({ role: 'assistant', content: result });
        return { text: result, ok: true };
      }
      return { text: fallbackText(), ok: false };
    } catch {
      return { text: fallbackText(), ok: false };
    } finally {
      clearTimeout(timer);
    }
  }

  clear(): void {
    this.history = [];
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/core/chat.test.ts`
Expected: PASS（4-6 个用例全绿）。

- [ ] **Step 5: 提交**

```bash
git add assets/scripts/core/chat.ts tests/core/chat.test.ts
git commit -m "feat: 新增小柒聊天核心逻辑（人设/历史/降级）"
```

---

### Task 2: 平台能力探测 `platform.ts`

**Files:**
- Modify: `assets/scripts/core/platform.ts`
- Test: `tests/core/platform.test.ts`

**Interfaces:**
- Consumes: 无新增依赖。
- Produces:
  - `export function supportsSpeechRecognition(): boolean`
  - `export function supportsSpeechSynthesis(): boolean`

- [ ] **Step 1: 写失败测试**（追加到 `tests/core/platform.test.ts`）

```ts
  it('语音能力探测：无对应环境 API 时返回 false', () => {
    // vitest/node 默认无 SpeechRecognition / speechSynthesis
    expect(supportsSpeechRecognition()).toBe(false);
    expect(supportsSpeechSynthesis()).toBe(false);
  });
```

（并把 `supportsSpeechRecognition, supportsSpeechSynthesis` 加入顶部 import。）

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/core/platform.test.ts`
Expected: FAIL（`supportsSpeechRecognition` is not exported）。

- [ ] **Step 3: 实现**（追加到 `assets/scripts/core/platform.ts` 末尾）

```ts
export function supportsSpeechRecognition(): boolean {
  const w = globalThis as any;
  return typeof (w.SpeechRecognition ?? w.webkitSpeechRecognition) === 'function';
}

export function supportsSpeechSynthesis(): boolean {
  const w = globalThis as any;
  return typeof w.speechSynthesis === 'object' && w.speechSynthesis !== null &&
    typeof w.speechSynthesis.speak === 'function';
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/core/platform.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add assets/scripts/core/platform.ts tests/core/platform.test.ts
git commit -m "feat: 新增语音能力探测"
```

---

### Task 3: 新增小柒表情帧美术清单

**Files:**
- Modify: `assets/scripts/core/art.ts`
- Test: `tests/core/art.test.ts`（可选，无新增断言可跳过）

**Interfaces:**
- Consumes: 无。
- Produces: `ART_MANIFEST` 新增 3 个 key：`char-xiaoqi-idle`、`char-xiaoqi-say`、`char-xiaoqi-happy`（category `character`）。`ArtService.preload` 会自动加载；缺图回退逻辑已在 `hasArt`/`makeSprite` 中。

- [ ] **Step 1: 编辑 `assets/scripts/core/art.ts`**，在 `char-ashen` 之后追加 3 行：

```ts
  { key: 'char-xiaoqi-idle',   category: 'character', w: 150, h: 150 },
  { key: 'char-xiaoqi-say',    category: 'character', w: 150, h: 150 },
  { key: 'char-xiaoqi-happy',  category: 'character', w: 150, h: 150 },
```

- [ ] **Step 2: 运行全量测试确认无回归**

Run: `npm test`
Expected: PASS。

- [ ] **Step 3: 提交**

```bash
git add assets/scripts/core/art.ts
git commit -m "feat: 增加小柒数字人表情帧美术清单"
```

> 注：三张 `char-xiaoqi-*.png` 出图与 `compress_art.py` 压缩、元数据 `.meta` 生成属美术管线步骤，见实现后置清单（本任务只登记清单项；若缺少 PNG，`hasArt` 返回 false 时 ChatView 回退到 `char-xiaoqi`/emoji，不影响功能）。

---

### Task 4: 聊天面板 `ui/ChatView.ts` + 接入 HUD/Main

**Files:**
- Create: `assets/scripts/ui/ChatView.ts`
- Modify: `assets/scripts/ui/HudView.ts`（新增聊天按钮回调）
- Modify: `assets/scripts/ui/Main.ts`（实例化 + 挂载）
- Test: 无（UI 层无单测，沿用现有方式；以构建/预览验证）

**Interfaces:**
- Consumes:
  - `ChatService`（Task 1）：`new ChatService({ baseUrl })`、`.send(input): Promise<ChatReply>`、`.history`、`.clear()`。
  - `Widgets`: `COLOR, makeNode, makeRect, roundRect, makeLabel, pillButton`。
  - `ArtService`: `makeSprite(parent, key, w, h, x, y, name)`、`hasArt(key)`、`panelWithArt`。
  - `platform.ts`（Task 2）：`supportsSpeechRecognition()`、`supportsSpeechSynthesis()`。
  - cc: `Button, Color, EditBox, Label, Node, Graphics, Vec3, tween`。
- Produces:
  - `new ChatView(parent: Node, service: ChatService, hasFetch: boolean)`：`open()` / `close()` / `isOpen: boolean`。
  - `HudView` 构造签名新增参数 `chatCb: () => void`（追加在 `mergeCb` 之后），并在顶部栏加「聊天」按钮。
  - `Main.ts`：定义 `CHAT_BASE`，构造 `ChatService` 与 `ChatView`，HUD 传入 `() => chatView.open()`。

- [ ] **Step 1: 新建 `assets/scripts/ui/ChatView.ts`**

在合成台/升级等既有 view 之后，按下述代码创建（面板复用 `panel-popup`；表情动画、语音、文字输入、消息列表内联实现）：

```ts
import { Button, Color, EditBox, Graphics, Label, Node, Vec3, tween } from 'cc';
import { COLOR, makeLabel, makeNode, makeRect, pillButton, roundRect } from './Widgets';
import { ArtService } from './ArtView';
import { supportsSpeechRecognition, supportsSpeechSynthesis } from '../core/platform';
import { ChatService, ChatReply } from '../core/chat';

const WELCOME_LINES = [
  '嗨，我是小柒！欢迎来我的小店里坐坐～',
  '今天想聊聊生活、做饭，还是带娃那些事儿？',
];
const FALLBACK_AVATAR = 'char-xiaoqi';
const MSG_WINDOW_H = 280;
const MSG_MAX = 9;

type Mood = 'idle' | 'say' | 'happy';

export class ChatView {
  isOpen = false;
  private overlay!: Node;
  private panel!: Node;
  private avatar!: Node;
  private avatarFallback!: Label;
  private moods: Mood[] = [];
  private mood = 'idle' as Mood;
  private floatT = 0;
  private sayT = 0;
  private msgList!: Node;
  private input!: EditBox;
  private micBtn!: Node;
  private sendBtn!: Node;
  private rec: any = null;
  private listening = false;
  private talking = false;
  private hasFetch: boolean;

  constructor(private parent: Node, private service: ChatService, hasFetch: boolean) {
    this.hasFetch = hasFetch;
    this.overlay = makeRect('chat-overlay', parent, 960, 640, 0, 0, new Color(0, 0, 0, 110));
    this.overlay.active = false;
    this.overlay.on(Node.EventType.TOUCH_START, () => this.close());

    this.panel = ArtService.panelWithArt('chat-panel', parent, 'panel-popup', 720, 520, 0, 0);
    this.panel.active = false;

    // 顶部标题栏：头像 + LIVE + 话题
    this.buildHeader();
    // 中部消息区
    this.msgList = makeNode('chat-msgs', this.panel, 640, MSG_WINDOW_H, 0, 30);
    this.clipMessages();
    // 底部输入区
    this.buildInputBar();

    // 欢迎语
    this.pushWelcome();
    this.rebuildMessages();
  }

  private buildHeader(): void {
    makeLabel('chat-title', this.panel, '🔴 LIVE · 小柒', 20, 0, 232, COLOR.text);
    const topic = roundRect('chat-topics', this.panel, 300, 30, 0, 196, COLOR.panel, COLOR.border);
    makeLabel('chat-topic-text', topic, '生活 · 做饭 · 育儿', 13, 0, 0, COLOR.subtext);

    this.avatar = makeNode('chat-avatar', this.panel, 120, 120, -270, 150);
    const g = this.avatar.addComponent(Graphics);
    g.fillColor = COLOR.panel;
    g.circle(0, 0, 60);
    g.fill();
    this.avatarFallback = makeLabel('chat-avatar-fallback', this.avatar, '🙋‍♀️', 40, 0, -10, COLOR.text);
  }

  // 详见下方定义区块：[applyAvatar / update]（含 curKey 去重）
  private buildInputBar(): void {
    const bar = roundRect('chat-input-bar', this.panel, 640, 54, 0, -216, COLOR.panel, COLOR.border);
    // 文字输入
    const box = makeNode('chat-edit', bar, 440, 38, -60, 0);
    this.input = box.addComponent(EditBox);
    this.input.placeholder = '和小柒说点什么…';
    this.input.fontSize = 16;
    this.input.maxLength = 200;
    this.input.string = '';

    // 麦克风（仅能力可用时显示）
    if (supportsSpeechRecognition() && this.hasFetch) {
      this.micBtn = pillButton('chat-mic', bar, 46, 38, 220, 0, COLOR.accent, '🎙', () => this.toggleMic());
    }
    // 发送
    this.sendBtn = roundRect('chat-send', bar, 64, 38, 285, 0, COLOR.primary, COLOR.white);
    this.sendBtn.addComponent(Button);
    makeLabel('chat-send-text', this.sendBtn, '发送', 16, 0, 0, COLOR.white);
    this.sendBtn.on(Button.EventType.CLICK, () => this.onSend());
  }

  private clipMessages(): void {
    const mask = this.msgList.addComponent(Graphics);
    mask.fillColor = COLOR.panel;
    mask.rect(-320, -MSG_WINDOW_H / 2, 640, MSG_WINDOW_H);
    mask.fill();
    this.msgList.addComponent(Graphics).fillColor = COLOR.panel; // keep bg under bubbles
  }

  private pushWelcome(): void {
    for (const w of WELCOME_LINES) {
      this.service.history.push({ role: 'assistant', content: w });
    }
    this.mood = 'happy';
  }

  private onSend(): void {
    const text = this.input.string.trim();
    if (!text) return;
    this.input.string = '';
    void this.dispatch(text);
  }

  private toggleMic(): void {
    if (this.listening) { this.stopRec(); return; }
    const w = globalThis as any;
    const Ctor = w.SpeechRecognition ?? w.webkitSpeechRecognition;
    if (!Ctor) return;
    this.rec = new Ctor();
    this.rec.lang = 'zh-CN';
    this.rec.interimResults = false;
    this.rec.onresult = (e: any) => {
      const t = e.results && e.results[0] && e.results[0][0] && e.results[0][0].transcript;
      if (t) {
        const hasText = this.input.string.length === 0;
        if (hasText) this.input.string = t;
        void this.dispatch(t);
      }
      this.stopRec();
    };
    this.rec.onerror = () => this.stopRec();
    this.rec.onend = () => this.stopRec();
    try {
      this.rec.start();
      this.listening = true;
      if (this.micBtn) this.micBtn.getComponentInChildren(Label)!.string = '⏹';
    } catch { this.stopRec(); }
  }

  private stopRec(): void {
    this.listening = false;
    try { this.rec?.stop(); } catch { /* ignore */ }
    this.rec = null;
    if (this.micBtn) this.micBtn.getComponentInChildren(Label)!.string = '🎙';
  }

  private async dispatch(text: string): Promise<void> {
    if (!this.hasFetch) {
      this.service.history.push({ role: 'assistant', content: '当前环境暂不支持联网聊天，我先陪你说句话吧～' });
      this.rebuildMessages();
      return;
    }
    this.setSendEnabled(false);
    this.mood = 'idle';
    const reply: ChatReply = await this.service.send(text);
    this.setSendEnabled(true);
    this.applyMoodByReply(reply);
    this.rebuildMessages();
    if (reply.ok) {
      this.stopRec();
      this.playVoice(reply.text);
    }
  }

  private applyMoodByReply(reply: ChatReply): void {
    if (reply.ok) {
      const pos = /开心|哈哈|真好|喜欢|棒|不错|谢谢|好呀|欢迎|加油|甜/i.test(reply.text);
      this.mood = pos ? 'happy' : 'say';
      this.sayT = 1.6;
    } else {
      this.mood = 'idle';
    }
  }

  private playVoice(text: string): void {
    if (!supportsSpeechSynthesis()) return;
    const synth = (globalThis as any).speechSynthesis;
    let voice: SpeechSynthesisVoice | null = null;
    for (const v of synth.getVoices()) {
      if (/zh/i.test(v.lang) && /female|woman|xiaoyi|tingting|huihui/i.test(v.name)) { voice = v; break; }
    }
    const u = new SpeechSynthesisUtterance(text);
    u.lang = 'zh-CN';
    if (voice) u.voice = voice;
    this.talking = true;
    this.mood = 'say';
    u.onend = () => { this.talking = false; };
    u.onerror = () => { this.talking = false; };
    synth.speak(u);
  }

  private setSendEnabled(on: boolean): void {
    const send = this.sendBtn.getComponent(Button);
    if (send) send.interactable = on;
  }

  private rebuildMessages(): void {
    this.msgList.removeAllChildren();
    const msgs = this.service.history.slice(-MSG_MAX);
    const startY = Math.min(MSG_WINDOW_H / 2 - 24, 100);
    msgs.forEach((m, i) => {
      const isUser = m.role === 'user';
      const width = Math.min(360, 24 + m.content.length * 17);
      const bubble = roundRect(
        `m-${i}`, this.msgList, width, 34, isUser ? 210 : -210, startY - i * 46, 12,
        isUser ? COLOR.primary : COLOR.panel,
        isUser ? undefined : COLOR.border,
      );
      const label = makeLabel(`mt-${i}`, bubble, m.content, 14, 0, 0,
        isUser ? COLOR.white : COLOR.text);
      label.overflow = 'NONE';
      // 简单换行支持：按 30 字断行
      const chunkSize = 22;
      if (m.content.length > chunkSize) {
        const wrapped = m.content.match(new RegExp(`.{1,${chunkSize}}`, 'g'))?.join('\n') ?? m.content;
        label.string = wrapped;
        bubble.getComponent(Graphics)!.clear(); // 重新绘制按内容高度微调省略，保持 MVP
        roundRect(`mb-${i}`, this.msgList, width, 30 + Math.ceil(wrapped.length / chunkSize) * 18,
          isUser ? 210 : -210, startY - i * (34 + Math.ceil(wrapped.length / chunkSize) * 22) - 6, 12,
          isUser ? COLOR.primary : COLOR.panel, isUser ? undefined : COLOR.border);
      }
    });
  }

  private curKey = '';
  private applyAvatar(mood: Mood): void {
    this.mood = mood;
    const key = (mood === 'say' && (this.talking || this.sayT > 0))
      ? 'char-xiaoqi-say'
      : mood === 'happy' ? 'char-xiaoqi-happy' : 'char-xiaoqi-idle';
    // 已挂该帧则不再重建，避免每帧重挂 Sprite
    if (this.curKey === key) return;
    this.curKey = key;
    this.avatar.removeAllChildren();
    let used = false;
    if (ArtService.hasArt(key)) {
      ArtService.makeSprite(this.avatar, key, 120, 120, 0, 0, 'avatar-frame');
      used = true;
    } else if (ArtService.hasArt(FALLBACK_AVATAR)) {
      ArtService.makeSprite(this.avatar, FALLBACK_AVATAR, 120, 120, 0, 0, 'avatar-frame');
      used = true;
    }
    this.avatarFallback.node.active = !used;
  }

  update(dt: number): void {
    if (!this.isOpen) return;
    this.floatT += dt;
    const dy = Math.sin(this.floatT * 2) * 4;
    this.avatar.setPosition(-270, 150 + dy, 0);
    if (this.sayT > 0) this.sayT -= dt;
    this.applyAvatar(this.mood);
    if (this.mood === 'happy') {
      tween(this.avatar)
        .to(0.2, { scale: new Vec3(1.08, 1.08, 1) })
        .to(0.2, { scale: new Vec3(1, 1, 1) })
        .start();
    }
  }

  open(): void {
    if (!this.hasFetch) {
      // 无 fetch 环境：仍可打开看欢迎语
    }
    this.isOpen = true;
    this.overlay.active = true;
    this.panel.active = true;
    this.mood = 'happy';
  }

  close(): void {
    this.isOpen = false;
    this.stopRec();
    const synth = (globalThis as any).speechSynthesis;
    if (synth && typeof synth.cancel === 'function') synth.cancel();
    this.talking = false;
    this.overlay.active = false;
    this.panel.active = false;
  }
}
```

- [ ] **Step 2: 修改 `assets/scripts/ui/HudView.ts`**（构造签名追加 `chatCb`，加按钮）

将构造签名 `constructor(parent: Node, upgradeCb: () => void, chapterCb: () => void, adCb: () => void, mergeCb: () => void)` 改为追加 `chatCb: () => void`；在右组「第1章」按钮附近/合成台附近加聊天按钮：

```ts
    pillButton('chat-btn', parent, 70, 34, 340, 293, COLOR.primary, '💬 聊天', chatCb);
```

（插入到 `pillButton('upgrade-btn', ...)` 之前，并把其 x 从 410 左移到 388，避免与聊天按钮重叠。）

- [ ] **Step 3: 修改 `assets/scripts/ui/Main.ts`**

顶部 import 处（其余 import 保持不变）追加：

```ts
import { ChatService } from '../core/chat';
import { ChatView } from './ChatView';
import { createKVStore } from '../core/platform';
```

在类内新增字段与常量：

```ts
const CHAT_BASE = 'https://twmedia.dpdns.org';
// 字段区新增：
private chatView!: ChatView;
```

在 `buildGame()` 中（`this.skinView = new SkinView(...)` 之后、`this.merge = new MergeView(...)` 之前）加入：

```ts
    const chatService = new ChatService({ baseUrl: CHAT_BASE });
    this.chatView = new ChatView(this.node, chatService, typeof fetch === 'function');
```

在 `buildGame()` 里 HUD 构造调用处（`this.hud = new HudView(this.node, ...)`）在其末尾追加聊天回调实参：

```ts
      () => this.chatView.open(),
```

（注意：HUD 在 `chatView` 实例化之前的构造列表里；因 `this.chatView.open` 是回调、运行时才调用，此时 `chatView` 已就绪，顺序无碍。也可直接在实参里写 `() => this.chatView.open()`。）

在 `update(dt)` 开头（`if (!this.ready) return;` 之后）加：

```ts
    this.chatView.update(dt);
```

- [ ] **Step 4: 校验无回归**（TypeScript 仅由 Cocos 编译，本地用 vitest 校验 core 层）

Run: `npm test`
Expected: PASS。

- [ ] **Step 5: Cocos 编译/预览冒烟**（人工步骤）

在 Cocos Creator 打开 `assets/scenes/main.scene` 预览：
1. 顶部栏出现「💬 聊天」按钮；点击弹出面板（带 LIVE·小柒 标题、话题标签、头像、欢迎语气泡、输入框、发送/麦克风按钮）。
2. 输入文字点「发送」→ 出现"回复中"后显示小柒回复气泡；后端可达时返回真实文本，不可达时显示降级文案。
3. 有麦克风权限时点 🎙 说话 → 识别文字并自动发送。
4. 面板关闭中断语音、头像回待机。
（此步需后端已部署 CORS，见 Task 5；若后端未就绪，聊天仍可打开并显示欢迎语与降级提示。）

- [ ] **Step 6: 提交**

```bash
git add assets/scripts/ui/ChatView.ts assets/scripts/ui/HudView.ts assets/scripts/ui/Main.ts
git commit -m "feat: 接入小柒直播聊天面板（HUD 入口+数字人动画）"
```

---

### Task 5: 后端 CORS（`HTWCore.Web`）

**Files:**
- Modify: `D:\HTWCore.Web\Startup.cs`

**Interfaces:**
- Consumes: 无。
- Produces: 浏览器可跨域访问 `POST /api/backend/chat`。允许来源：`https://htwmedia.github.io` + 本机预览端口。

- [ ] **Step 1: `Startup.ConfigureServices` 末尾新增 CORS**

在 `ConfigureServices` 末尾（`services.AddScoped<ComplianceService>();` 之后）插入：

```csharp
            services.AddCors(o => o.AddPolicy("GameCors", p => p
                .SetIsOriginAllowed(origin =>
                {
                    if (origin == "https://htwmedia.github.io") return true;
                    if (Uri.TryCreate(origin, UriKind.Absolute, out var u))
                    {
                        if (u.Scheme == "http" && (u.Host == "localhost" || u.Host == "127.0.0.1")) return true;
                    }
                    return false;
                })
                .AllowAnyHeader()
                .AllowAnyMethod()));
```

- [ ] **Step 2: `Startup.Configure` 启用 CORS**

在 `app.UseRouting();` 之后、`app.UseAuthentication();` 之前插入：

```csharp
            app.UseCors("GameCors");
```

- [ ] **Step 3: 编译校验**

Run（在 `D:\HTWCore.Web` 目录）: `dotnet build`
Expected: Build succeeded（无错误）。

- [ ] **Step 4: 部署**（人工步骤）

将重建产物部署到 `twmedia.dpdns.org`，并冒烟：浏览器控制台对
`https://twmedia.dpdns.org/api/backend/chat` 发跨域 `POST`，观察响应头含 `Access-Control-Allow-Origin`。

- [ ] **Step 5: 提交**

```bash
git add Startup.cs
git commit -m "feat: 允许游戏站跨域调用聊天接口"
```

> 后端仓库独立于本游戏仓库，该 commit 在 `D:\HTWCore.Web` 下执行。

---

## 实现后置清单（人工/美术，非代码任务）

1. 用项目水彩出图管线生成 `char-xiaoqi-idle.png / char-xiaoqi-say.png / char-xiaoqi-happy.png`（150×150，透明背景），放入 `assets/resources/art/`，压缩并提交 `.png` + `.meta`（参考 `tools/compress_art.py` 与既有 `char-xiaoqi.png`）。
2. 部署后端到 `twmedia.dpdns.org`（Task 5）后再发布 GH Pages 试玩（`tools/release-play.ps1` 或 CI）。
